pipeline {
    agent {
        docker { image 'node:23.3.0-alpine' }
    }
    stages {
        stage('Test') {
            steps {
                sh 'node --eval "console.log(process.platform,process.env.CI)"'
            }
        }
    }
}
